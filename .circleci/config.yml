version: 2.1
# docker run --name pcmd --rm -it circleci/python ls
jobs:
  idemo:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: ls
workflows:
  version: 2
  run1: 
    jobs:
      - idemo:
          context: kon
          filters:
            branches:
              only: master